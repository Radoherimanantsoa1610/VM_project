import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';
import './assets/styles/main.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

// La ligne suivante est commentée, donc vous pouvez la laisser ainsi si vous ne souhaitez pas utiliser reportWebVitals
// reportWebVitals(); 

